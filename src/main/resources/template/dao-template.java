package ${daoPackage};

import ${fullQueryClass};
import ${fullVoClass};

import java.util.List;

public interface ${daoClass} {

    int getCount(${queryClass} ${queryVar});

    List<${voClass}> getList(${queryClass} ${queryVar});

}
